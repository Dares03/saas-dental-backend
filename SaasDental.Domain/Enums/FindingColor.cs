namespace SaasDental.Domain.Enums;

public enum FindingColor
{
    Red = 1, // Mal estado / Patología (e.g., Caries activa, Corona en mal estado)
    Blue = 2 // Buen estado / Tratamiento realizado (e.g., Curación terminada, Corona en buen estado)
}

namespace SaasDental.Domain.Enums;

public enum TransactionType
{
    Income = 1, // Ingreso (Cobros a pacientes, ingresos extra)
    Expense = 2 // Egreso (Pago a proveedores, servicios de la sede)
}
